name: Retired refresh-directional-readout
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use Canonical Metals Test Build Publish. This legacy writer is retired."
